services:
  - type: web
    name: disaster-response-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app/run.py"
    envVars:
      - key: FLASK_APP
        value: "app/run.py"
      - key: FLASK_ENV
        value: "production"
